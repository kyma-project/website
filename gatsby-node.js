const {
  onCreateWebpackConfig,
  createPages,
  onCreateNode,
  onCreatePage,
} = require("./gatsby");

module.exports = {
  onCreateWebpackConfig,
  createPages,
  onCreateNode,
  onCreatePage,
};
