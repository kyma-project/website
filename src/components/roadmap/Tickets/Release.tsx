import React from "react";

import Grid from "@styled/Grid";

import { Release } from "../types";

interface ReleaseProps {
  release: Release;
}

const ReleaseComponent: React.FunctionComponent<ReleaseProps> = ({ release }) =>
  null;

export default ReleaseComponent;
