import styled from "@styled";

export const ToastsContainer = styled.div`
  position: fixed;
  bottom: 16px;
  right: 16px;
`;

export const Toast = styled.div`
  margin-top: 16px;
  padding: 16px;
`;
